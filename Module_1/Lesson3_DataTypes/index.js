// Task #2

let maxSize = 64;
let labelText = 'T-shirts';
let saleDiscount = true;

console.log('maxSize=', maxSize, typeof maxSize);
console.log('labelText=', labelText, typeof labelText);
console.log('saleDiscount=', saleDiscount, typeof saleDiscount);

maxSize = 'XXL';
alert(maxSize);

labelText = 278;
alert(labelText);

saleDiscount = null;
alert(saleDiscount);
